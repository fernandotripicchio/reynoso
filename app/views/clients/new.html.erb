<div id="edit_form" class="form_user">  	
	<legend>Nuevo Cliente:</legend>
	<div class="row-horizontal">
	  	<%= simple_form_for @client ,  :url => branch_clients_path(@branch, @client ) ,:html => { :class => "form-horizontal" } do |f| %>
	  	<%= render :partial => "form", :locals => {:f => f}%>
	  	<hr />
	  	<%= link_to "Cancelar", branch_clients_path(@branch), :class => "btn btn-secundary btn-form"%>
	    <%= f.submit "Guardar", :class => "btn btn-primary btn-form"%>
	    <% end %>
	</div>
</div>

