<div id="edit_form" class="form_user">  	
	<legend>Sucursarl <%= @branch.name %> - Agregar Stock:</legend>
	<div class="row-horizontal">
	  	<%= simple_form_for @stock, :url => branch_actualizar_stock_path(@branch) ,  :html => { :class => "form-horizontal" } do |f| %>
	  	<%= render :partial => "form", :locals => {:f => f, :editar => false}%>
	  	<hr />
	  	<%= link_to "Cancelar", branch_stocks_path(@branch), :class => "btn btn-secundary btn-form"%>
	    <%= f.submit "Guardar", :class => "btn btn-primary btn-form"%>
	    <% end %>
	</div>
</div>

